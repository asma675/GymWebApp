@{
    ViewBag.Title = "Admin";
}
<h2>Admin</h2>
<p>
    <a class="btn btn-secondary">Member Directory</a>
    <a class="btn btn-outline-secondary">Find a Member</a>
</p>

@using (Html.BeginForm("Logout",$"Admin",FormMethod.Post))
{
    @Html.AntiForgeryToken()
    <button class="btn btn-link">Sign out</button>
}